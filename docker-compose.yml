version: '3.8'

services:
  app:
    build: .
    ports:
      - "3030:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:/app/data/prod.db
      - ADMIN_PASSWORD=admin12345
    volumes:
      - db_data:/app/data
    restart: unless-stopped

volumes:
  db_data:
